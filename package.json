{
  "name": "redux-p",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest"
  },
  "lint-staged": {
    "src/**/*.js": [
      "prettier --single-quote --trailing-comma --parser=flow --write --printWidth=120",
      "eslint --fix",
      "git add"
    ]
  },
  "jest": {
    "rootDir": "src"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^3.19.0",
    "eslint-plugin-import": "^2.2.0",
    "flow-bin": "^0.42.0",
    "husky": "^0.13.3",
    "jest": "^19.0.2",
    "lint-staged": "^3.4.0",
    "prettier": "^0.22.0"
  }
}
